# invoke with: ansible-playbook ianseobox.yml
# on macOS with ansible 2.3.1 and later, you have to install sshpass:
#   brew tap esolitos/ipa
#   brew install sshpass
---

- import_playbook: installsshkeys0.yml
- import_playbook: buildianseobox.yml
